import { MemberEdit.Resolver } from './member-edit.resolver';

describe('MemberEdit.Resolver', () => {
  it('should create an instance', () => {
    expect(new MemberEdit.Resolver()).toBeTruthy();
  });
});
