import { MemberDetail.Resolver } from './member-detail.resolver';

describe('MemberDetail.Resolver', () => {
  it('should create an instance', () => {
    expect(new MemberDetail.Resolver()).toBeTruthy();
  });
});
