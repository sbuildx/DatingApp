import { MemberList.Resolver } from './member-list.resolver';

describe('MemberList.Resolver', () => {
  it('should create an instance', () => {
    expect(new MemberList.Resolver()).toBeTruthy();
  });
});
